type User {
  id: ID!
  name: String!
  teamName: String!
}

type Team {
  id: ID!
  name: String!
}

type Mutation {
  addUser(name: String!): User
}

type Query {
  users: [User!]!
  teams: [Team!]!
  user(name: String!): User
  # userById(id: String!): User
}
